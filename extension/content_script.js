
// literally terrible

// function f() {
// 	$(":visible").each(function(index, elem) {
// 		var offset = $(elem).offset();
// 		offset.left += 0.2 * (Math.random() - 0.5);
// 		offset.top += 0.2 * (Math.random() - 0.5);
// 		$(elem).offset(offset);
// 	});
// }

// var t = setInterval(f, 100);